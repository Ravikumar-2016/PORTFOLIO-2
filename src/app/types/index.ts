export interface Skill {
    name: string
    level: number
  }
  
  export interface Project {
    title: string
    description: string
    imageUrl: string
  }
  
  export interface Education {
    institution: string
    period: string
    description: string
  }
  
  